git config --global user.name "RubensZimbres"

git config --global user.email "rubens.zimbres@eclipso.de"

git init

git remote add origin https://github.com/RubensZimbres/TestePush

git pull

git status

git add Hello1.py

git commit -m "initial commit"

git push -u origin master

# alter Hello1.py

git diff origin/master

--- a/Hello1.py
+++ b/Hello1.py
@@ -1 +1 @@
-print("Hello World 22")
+print("Hello World 44")

git commit -m "added while"

git push -u origin master

git log Hello1.py




### DEVELOP -- RESTORE PREVIOUS VERSION

git checkout -b c9f55653

git commit -m "restoring previous version 22"

git cherry-pick c9f5565.....

git checkout origin/master

git add Hello1.py

git push -u origin master

